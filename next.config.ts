import type { NextConfig } from "next";

/**
 * Next.js configuration object
 * Currently using default settings with placeholder for future customizations
 */
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
