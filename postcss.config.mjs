/**
 * PostCSS configuration for Tailwind CSS processing
 * Enables Tailwind CSS utility classes and custom CSS processing
 */
const config = {
  plugins: {
    // Tailwind CSS plugin for utility class generation and optimization
    "@tailwindcss/postcss": {},
  },
};

export default config;
